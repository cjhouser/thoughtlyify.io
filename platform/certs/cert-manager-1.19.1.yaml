global:
  logLevel: 2
  leaderElection:
    namespace: certs
crds:
  enabled: true
  keep: true
extraArgs:
- --logging-format=json
resources:
  limits:
    memory: 64Mi
  requests:
    cpu: 80m
    memory: 64Mi
prometheus:
  enabled: false

webhook:
  extraArgs:
  - --logging-format=json
  resources:
    limits:
      memory: 64Mi
    requests:
      cpu: 80m

cainjector:
  extraArgs:
  - --logging-format=json
  resources:
    limits:
      memory: 64Mi
    requests:
      cpu: 80m

startupapicheck:
  resources:
    limits:
      memory: 64Mi
    requests:
      cpu: 80m
